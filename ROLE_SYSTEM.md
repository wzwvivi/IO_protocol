# 角色权限系统说明

## 三种角色

### 1. 普通账户 (viewer)
- **权限**: 只能浏览，不能修改
- **可访问页面**: 设备树、Label 列表、代码预览
- **不可操作**: 添加/删除设备、添加/删除 Label、保存修改
- **显示标签**: 蓝色 "普通账户"

### 2. 设备人员 (operator)
- **权限**: 可以浏览和修改协议
- **可访问页面**: 所有页面
- **可操作**: 
  - 添加/删除设备
  - 添加/删除 Label
  - 保存协议修改
  - 生成代码
- **不可操作**: 用户管理（添加/删除用户）
- **显示标签**: 绿色 "设备人员"

### 3. 管理员 (admin)
- **权限**: 全部权限
- **可访问页面**: 所有页面
- **可操作**: 
  - 设备人员的所有操作
  - 用户管理（添加/删除/修改用户）
  - 设置用户角色
- **显示标签**: 紫色 "管理员"

## 管理员添加用户流程

1. 管理员登录后，点击右上角用户菜单中的 "用户管理"
2. 点击 "创建用户" 按钮
3. 填写用户信息：
   - 用户名
   - 密码
   - 显示名称
   - 邮箱（可选）
   - **角色**（关键！）：
     - 普通账户（只读）- 只能浏览
     - 设备人员（可修改）- 可以修改协议
     - 管理员（全部权限）- 可以管理用户
4. 点击 "创建"

## 技术实现

### 后端权限装饰器
- `@admin_required`: 仅管理员可访问
- `@operator_required`: 设备人员和管理员可访问
- `@viewer_required`: 所有登录用户可访问

### 受保护的 API
- `POST /api/device/<id>/labels` - 保存 Labels（需 operator）
- `DELETE /api/device/<id>` - 删除设备（需 operator）
- `DELETE /api/device/<id>/version/<version>` - 删除版本（需 operator）
- `POST /api/add_device` - 添加设备（需 operator）
- `POST /api/add_system` - 添加系统（需 operator）

### 前端权限控制
- 使用 `canEdit` 变量判断是否有编辑权限
- 根据 `currentUserRole` 显示/隐藏编辑按钮
- 保存操作前进行客户端权限检查
